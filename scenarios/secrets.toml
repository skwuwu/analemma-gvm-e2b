# Demo credentials — fake keys for the echo server only.
# Never use real API keys in this file.

[credentials."demo.api.example.com"]
type  = "Bearer"
token = "demo-token-not-real-replace-in-production"
